#include "uls.h"

void mx_clear_list(t_list *list) {
    // t_list *p1 = list;
    // t_list *p2 = list;

    // while (p1 != NULL) {
    //     p1 = p1->next->next;
    //     p2 = p2->next;
    //     p1->data = NULL;
    // }
}
